<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks plain HTTP by default, which is right. The local dev backend is
  plain HTTP, so exactly those hosts are excepted and nothing else:
    10.0.2.2  - the emulator's alias for your development machine
    localhost - a device forwarding a port with `adb reverse`
  Production traffic stays HTTPS-only, because the default below is unchanged.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
